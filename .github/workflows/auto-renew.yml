name: Weirdhost 续期

on:
  schedule:
    - cron: '0 8 * * 0'   # 每周日 UTC 08:00（韩国时间 17:00）
  workflow_dispatch:     # 允许手动触发

jobs:
  login-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # 设置30分钟超时，避免GitHub Actions默认6小时太长

    # 授予工作流写入仓库内容的权限
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install playwright
        playwright install chromium

    - name: Set up WARP
      uses: fscarmen/warp-on-actions@v1.4
      with:
        stack: dual        # ipv4 / ipv6 / dual
        mode: wireguard   # wireguard / client

    - name: Run auto renewal
      env:
        REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
        WEIRDHOST_EMAIL: ${{ secrets.WEIRDHOST_EMAIL }}
        WEIRDHOST_PASSWORD: ${{ secrets.WEIRDHOST_PASSWORD }}
        WEIRDHOST_SERVER_URLS: ${{ secrets.WEIRDHOST_SERVER_URLS }}
      run: python weird/write.py

    - name: Commit README and time file
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

        # 添加 weird 目录下的 README 和 time 文件
        git add weird/README.md weird/time.txt

        # 检查是否有变化需要提交
        if git diff --cached --quiet; then
          echo "README 或 time 文件无变化，无需提交"
        else
          git commit -m "📝 更新运行状态: $(date +'%Y-%m-%d %H:%M:%S')"
          git push origin HEAD:main
        fi
